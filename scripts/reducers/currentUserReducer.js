const initialState = {
  data: null,
};

const currentUserReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default currentUserReducer;
